import 'package:flutter/material.dart';

class NewFlutterScreen extends StatelessWidget {
  const NewFlutterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
